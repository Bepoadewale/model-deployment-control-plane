# Validation

Run `PYTHONPATH=control-plane/src python3 -m pytest -q` and lint once configured. Any delivery claim must retain records of model digest, evaluation inputs, routing configuration, gate result and post-rollback verification.
